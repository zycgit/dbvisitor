<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.hasor.dbvisitor.test.oneapi.realdb.milvus.dto3.UserInfoMilvus4Mapper">
    <insert id="insertUser">
        INSERT INTO tb_mapper_user_milvus (uid, name, loginName, loginPassword, v)
        VALUES (#{info.uid}, #{info.name}, #{info.loginName}, #{info.loginPassword}, #{info.v})
    </insert>

    <delete id="deleteUser">
        DELETE FROM tb_mapper_user_milvus WHERE uid = #{uid}
    </delete>

    <select id="findUser" resultType="net.hasor.dbvisitor.test.oneapi.realdb.milvus.dto1.UserInfoMilvus1">
        /*+ consistency_level=Strong */ SELECT * FROM tb_mapper_user_milvus WHERE uid = #{uid}
    </select>
</mapper>
