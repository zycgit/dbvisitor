POST /index/_doc/1
{
  ?: "value_is_string",
  "key_is_string": ?,
  ?: ?,
  "nested": {
    ?: "inner_key_is_placeholder"
  },
  "array_test": [?, "val", ?]
}

PUT /index/_doc/2 ?

POST /_search
{
    "query": {
        "term": {
            ?: ?
        }
    }
}
