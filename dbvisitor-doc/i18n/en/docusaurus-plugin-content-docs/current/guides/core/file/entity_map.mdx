---
id: entity_map
sidebar_position: 6
hide_table_of_contents: true
title: Mapping Tables
description: Map tables and results via entity and resultMap tags in mapper XML.
---
:::tip[Hint]
This article is generated by AI translation.
:::

import TagRed from '@site/src/components/tags/TagRed';
import TagGray from '@site/src/components/tags/TagGray';


# Mapping Tables

Use either `<resultMap>` or `<entity>` to map query results:
- [&lt;resultMap&gt;](./result_map): map SQL result sets.
- [&lt;entity&gt;](./entity_map): map a concrete table.

:::tip[How to choose between resultMap and entity?]
- For the same table with different query shapes, `entity` is good when columns all come from one table; `resultMap` when columns span multiple tables.
- Using the [Fluent API](../lambda/about) requires `entity` or [Object Mapping](../mapping/about) annotations.
- If the SELECT uses aliases, functions, CASE, or columns from multiple tables, `resultMap` is safer.
:::

## entity tag

```xml title='Define and use entity'
<entity id="user_resultMap" table="users" type="com.example.dto.UserBean">
    <id column="id" property="id"/>
    <mapping column="name" property="name"/>
    <mapping column="age" property="age"/>
    <mapping column="create_time" property="createTime"/>
</entity>

<select id="queryById" resultMap="user_resultMap">
    select
        id, name, age, create_time
    from
        users
    where
        id = #{id}
</select>
```

### Attributes

*entity tag*

| Property                  | Description                                                                                                                                          |
|---------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------|
| id                        | <TagGray/> Identifier; if empty, `type` is used.                                                                                                    |
| catalog                   | <TagGray/> Catalog of the mapped table.                                                                                                             |
| schema                    | <TagGray/> Schema of the mapped table.                                                                                                              |
| table                     | <TagGray/> Table name.                                                                                                                              |
| type                      | <TagRed/> Fully qualified Java type to map to.                                                                                                      |
| caseInsensitive           | <TagGray/> Case-insensitive mapping for column/property names. Default `true`.                                                                      |
| autoMapping               | <TagGray/> Whether to perform [auto-mapping](./automapping). Default `true`.                                                                        |
| mapUnderscoreToCamelCase  | <TagGray/> With auto-mapping, convert camelCase property names to snake_case columns (e.g., `createTime` → `create_time`). Default `false`.          |
| useDelimited              | <TagGray/> Force quoting/delimiters on table/column names when generating SQL. Default `false`.                                                     |
| character-set             | <TagGray/> Table character set (available via TableDescription).                                                                                    |
| collation                 | <TagGray/> Table collation (TableDescription).                                                                                                      |
| comment                   | <TagGray/> Table comment (TableDescription).                                                                                                        |
| other                     | <TagGray/> Other table options (TableDescription).                                                                                                   |
| ddlAuto                   | <TagGray/> Auto DDL strategy: `none`, `create`, `add`, `update`, `create-drop` (auto DDL not yet supported).                                        |

**id tag** and **mapping tag**

| Property             | Description                                                                                                                                                           |
|----------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| column               | <TagRed/> Column name in the result set.                                                                                                                              |
| property             | <TagRed/> Property name in the target type.                                                                                                                           |
| javaType             | <TagGray/> Usually inferred; specify when the property is abstract or an interface.                                                                                   |
| jdbcType             | <TagGray/> Overrides the default [Java↔JDBC mapping](../../types/java-jdbc).                                                                                          |
| typeHandler          | <TagGray/> Override the [type handler](../../types/about) to use.                                                                                                     |
| keyType              | <TagGray/> For [Fluent API](../lambda/about) only. Key generation strategy when the value is null. See [key generator](../mapping/keytype#xml).                      |
| insert               | <TagGray/> For [Fluent API](../lambda/about) only. Whether it participates in insert. See [write policy](../mapping/write_policy).                                    |
| update               | <TagGray/> For [Fluent API](../lambda/about) only. Whether it participates in update. See [write policy](../mapping/write_policy).                                    |
| selectTemplate       | <TagGray/> For [Fluent API](../lambda/about) only. Column expression when used in SELECT. Default empty (column name). See [templates](../mapping/template).          |
| insertTemplate       | <TagGray/> For [Fluent API](../lambda/about) only. Argument expression for INSERT. Default `?`. See [templates](../mapping/template).                               |
| setValueTemplate     | <TagGray/> For [Fluent API](../lambda/about) only. Argument expression in UPDATE SET. Default `?`. See [templates](../mapping/template).                            |
| whereColTemplate     | <TagGray/> For [Fluent API](../lambda/about) only. Column expression in WHERE for update/delete. Default empty (column name). See [templates](../mapping/template).   |
| whereValueTemplate   | <TagGray/> For [Fluent API](../lambda/about) only. Argument expression in WHERE for update/delete. Default `?`. See [templates](../mapping/template).               |
| orderByColTemplate   | <TagGray/> For [Fluent API](../lambda/about) only. Column expression in ORDER BY. Default empty (column name). See [templates](../mapping/template).                  |
| sqlType              | <TagGray/> Column data type (TableDescription).                                                                                                                      |
| length               | <TagGray/> Column length (TableDescription).                                                                                                                         |
| precision            | <TagGray/> Numeric precision (TableDescription).                                                                                                                    |
| scale                | <TagGray/> Numeric scale (TableDescription).                                                                                                                        |
| character-set        | <TagGray/> Column character set (TableDescription).                                                                                                                 |
| collation            | <TagGray/> Column collation (TableDescription).                                                                                                                     |
| nullable             | <TagGray/> Whether the column allows NULL; for &lt;id&gt; this is always true and cannot be set (TableDescription).                                                  |
| default              | <TagGray/> Column default value (TableDescription).                                                                                                                 |
| comment              | <TagGray/> Column comment (TableDescription).                                                                                                                       |
| other                | <TagGray/> Other column options (TableDescription).                                                                                                                 |
