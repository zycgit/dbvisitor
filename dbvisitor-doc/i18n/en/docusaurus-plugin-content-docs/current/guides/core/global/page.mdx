---
id: page
sidebar_position: 5.7
hide_table_of_contents: true
title: Pagination Object
description: Use dbVisitor ORM's pagination utilities to query data.
---
:::tip[Hint]
This article is generated by AI translation.
:::

dbVisitor ships with `PageObject`, an implementation of the `Page` interface that exposes handy pagination properties and methods.

export const HighAttr = () => (
    <span style={{ backgroundColor: 'rgb(227 153 17)', color: '#fff', borderRadius: '2px', padding: '0.2rem'}}>Attribute</span>
);


export const HighMethod = () => (
    <span style={{ backgroundColor: 'rgb(17 179 227)', color: '#fff', borderRadius: '2px', padding: '0.2rem'}}>Method</span>
);

| Name                                            | Description                                                                                          |
|-------------------------------------------------|------------------------------------------------------------------------------------------------------|
| <HighAttr/> `pageSize`                           | Page size; default `-1` means unlimited                                                              |
| <HighAttr/> `currentPage`                        | Current page number                                                                                  |
| <HighAttr/> `pageNumberOffset`                   | Page-number offset (set to `1` if your first page is 1, otherwise page 0 is the first page)           |
| <HighMethod/> `long getFirstRecordPosition()`    | Index of the first record on the current page                                                        |
| <HighMethod/> `long getTotalPage()`              | Total number of pages                                                                                |
| <HighMethod/> `long getTotalCount()`             | Total number of records                                                                              |
| <HighMethod/> `long firstPage()`                 | Jump to the first page                                                                               |
| <HighMethod/> `void previousPage()`              | Move to the previous page                                                                            |
| <HighMethod/> `void nextPage()`                  | Move to the next page                                                                                |
| <HighMethod/> `void lastPage()`                  | Jump to the last page                                                                                |
| <HighMethod/> `Map<String, Object> toPageInfo()` | Return pagination metadata as a map                                                                  |

`toPageInfo` returns a `Map` like the following:

```js
{
    "enable" : true,
    "pageSize" : 20,
    "totalCount" : 200,
    "totalPage" : 10,
    "currentPage" : 0,
    "recordPosition" : 0
}
```

## Custom Dialects

Five interfaces shape dialect customizations, all extending `SqlDialect`:

- `SqlDialect`: base interface for keywords, table/column names, and ORDER BY fragments
- `ConditionSqlDialect`: condition helpers such as `LIKE`
- `InsertSqlDialect`: advanced INSERT generation (e.g., [conflict handling](../lambda/insert#conflict))
- `PageSqlDialect`: pagination SQL generation
- `SeqSqlDialect`: sequence-related SQL

:::info[Tip]
Extend `AbstractDialect` and implement `PageSqlDialect` to build your own pagination dialect.
- `countSql`: generate the COUNT query
- `pageSql`: generate the paginated query
:::

```java title='Register a custom dialect'
SqlDialectRegister.registerDialectAlias(JdbcUtils.MYSQL, MyDialect.class);
```
