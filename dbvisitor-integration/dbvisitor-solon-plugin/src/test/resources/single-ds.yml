solon.dataSources:
  default!:
    class: "com.zaxxer.hikari.HikariDataSource"
    jdbcUrl: "jdbc:h2:mem:solon_single;MODE=MySQL;DB_CLOSE_DELAY=-1;USER=sa"
dbvisitor:
  default:
    mapperLocations: classpath:dbvisitor/mapper/*.xml
    mapperPackages: net.hasor.dbvisitor.test.dao
